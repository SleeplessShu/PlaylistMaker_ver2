<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/title_application_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/PlaylistMaker"
        tools:targetApi="31">
        <activity
            android:name=".settings.ui.ActivitySettings"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".old.ui.mediateka.ActivityMediateka"
            android:exported="false"
            android:label="@string/title_activity_mediateka" />
        <activity
            android:name=".old.ui.search.ActivitySearch"
            android:exported="false"
            android:label="@string/title_activity_search" />
        <activity
            android:name=".old.ui.main.ActivityMain"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".old.ui.player.ActivityPlayer"
            android:exported="false"
            android:label="@string/title_activity_player" />
    </application>

</manifest>